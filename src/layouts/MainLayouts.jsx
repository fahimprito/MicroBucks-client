import { Outlet } from "react-router-dom";

const MainLayouts = () => {
    return (
        <div>
            MainLayouts
            <Outlet></Outlet>
        </div>
    );
};

export default MainLayouts;